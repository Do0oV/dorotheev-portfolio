{{ form_start(form) }}

    {{ form_errors(form) }}

    {{ form_row(form.email) }}
    {{ form_row(form.message) }}

    <button class="btn">{{ button_label|default('Send') }}</button>

{{ form_end(form) }}
